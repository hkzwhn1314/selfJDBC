<mapper namespace="user">
    <select id="selectList">
        select * from product
    </select>

    <select id="selectOne">
        select * from product where id = ? and username = ?
    </select>
</mapper>